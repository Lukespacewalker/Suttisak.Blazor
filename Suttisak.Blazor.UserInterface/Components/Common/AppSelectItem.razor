@using System.Globalization
@typeparam TValue

<option value="@BindConverter.FormatValue(Value, CultureInfo.CurrentCulture)" disabled="@Disabled" selected="@(MultiSelect?.IsSelected(Value) == true)">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @Label
    }
</option>

@code {
    [CascadingParameter] private AppMultiSelect<TValue>? MultiSelect { get; set; }
    [Parameter, EditorRequired] public TValue Value { get; set; } = default!;
    [Parameter] public string? Label { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
