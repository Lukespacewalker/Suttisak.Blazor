@* @inherits FluentNav
<div class="navmenu">
    <input type="checkbox" title="Menu expand/collapse toggle" id="@_menuGuid" class="navmenu-icon navmenu-toggle" />
    <label for="@_menuGuid" class="navmenu-icon"><FluentIcon Value="@(new Icons.Regular.Size20.Navigation())" Color="Color.Fill" /></label>
    <nav aria-labelledby="main-menu" onclick="document.getElementById('@_menuGuid').click();">
        @{
            base.BuildRenderTree(__builder);
        }
    </nav>
</div>

@code {
    private Guid _menuGuid;

    protected override void OnInitialized()
    {
        base.OnInitialized();
        _menuGuid = Guid.CreateVersion7();
    }

} *@